function stringSlice(targetStr){

    var arrayChar = [];

    for(var i=0; i<targetStr.length;i++){
        
        arrayChar.push(targetStr.charAt(i));

    }

    return arrayChar;
    
};